/**
 * KPI cards visual component.
 *
 * Purpose:
 * - Render top-level financial metrics (income, expenses, net cash flow, savings rate).
 * - Provide quick at-a-glance status of user's financial health.
 */

import { DashboardKpis } from '../../../shared/src/contracts';

type Props = { kpis: DashboardKpis };

export function KpiCards(_props: Props) {
  // TODO: render responsive card grid for KPI metrics.
  return null;
}
